name: CI

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write
  pull-requests: write

jobs:
  ci:
    uses: input-output-hk/catalyst-forge/.github/workflows/ci.yml@debug-skip-github
    with:
      forge_version: 0.19.1
